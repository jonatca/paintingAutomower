def get_comb_cut_order():
    # width of one comb is 5.5 m
    # hej
    shortside_right = {"start": (0, 0), "end": (10, 0), "type": "line"}
    comb_cut_order = [shortside_right]
    return comb_cut_order
